
const Validation=(input)=>{
    let error={}
    
    // if (input.form.name.length<1){ error.nombre="El nombre no puede estar vacío"}
    
    // if (!input.dificultad){ error.dificultad="Elija un grado de dificultad"}

    // if (input.duracion<=0|| input.duracion.length >=3){error.duracion="La duracion debe ser mayor a 0 y no puede tener mas de dos cifras"}

    // if (!input.estacion){ error.temporada="Seleccione una estación"}
    

    
    


    
    return error
}

export default Validation;